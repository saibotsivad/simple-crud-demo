const validator = require('is-my-json-valid')
const validate = validator(require('common/schema/user.json'))

module.exports = options => (req, res, body) => {
	const username = req.params[0]
	const json = req.body
	const valid = validate(json)
	res.send(`is valid? ${valid}`)
}
