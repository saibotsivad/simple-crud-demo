module.exports = (connection, query) => {
	if (typeof query === 'undefined') {
		query = {}
	}

	return {
		select: () => {},
		from: () => {},
		where: () => {},
		whereAfter: () => {},
		build: () => {},
		execute: () => {}
	}
}
